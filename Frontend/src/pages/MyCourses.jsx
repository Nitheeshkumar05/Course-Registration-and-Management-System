import { useEffect, useState } from 'react'; import { getMyCourses } from '../api';
function MyCourses(){const [courses,setCourses]=useState([]),[error,setError]=useState(''); useEffect(()=>{getMyCourses().then(setCourses).catch(e=>setError(e.message))},[]); return <div className="content-page"><div className="section-heading"><div><p className="eyebrow">YOUR LEARNING</p><h1>My courses</h1><p className="muted">Your registered courses are shown here.</p></div></div>{error&&<div className="alert error-message">{error}</div>}<div className="course-grid">{courses.length===0?<div className="empty-card">You haven't registered for any courses yet.</div>:courses.map((c,i)=><div className="course-card" key={c.id??i}><div className="course-code">REGISTERED</div><h3>{c.courseName}</h3><p>{c.name} · {c.emailId}</p></div>)}</div></div>}; export default MyCourses;
