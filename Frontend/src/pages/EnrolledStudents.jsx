import { useEffect,useState } from 'react'; import { getEnrolledStudents } from '../api';
function EnrolledStudents(){const [students,setStudents]=useState([]),[error,setError]=useState(''); useEffect(()=>{getEnrolledStudents().then(setStudents).catch(e=>setError(e.message))},[]); return <div className="content-page"><div className="section-heading"><div><p className="eyebrow">ADMINISTRATION</p><h1>Student enrollments</h1><p className="muted">Review course registrations across all students.</p></div></div>{error&&<div className="alert error-message">{error}</div>}<div className="table-card"><table><thead><tr><th>Student</th><th>Email</th><th>Course</th></tr></thead><tbody>{students.map((s,i)=><tr key={s.id??i}><td>{s.name}</td><td>{s.emailId}</td><td><span className="table-pill">{s.courseName}</span></td></tr>)}</tbody></table></div></div>}; export default EnrolledStudents;
